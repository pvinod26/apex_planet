# apex_planet
The Online Voting System is a web-based application developed using PHP and MySQL that allows users to securely register, log in, and cast their vote for candidates. The system ensures that each user can vote only once by maintaining a voting status in the database
